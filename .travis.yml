language: ruby
rvm:
- 2.1

before_script:
 - chmod +x ./script/cibuild # or do this locally and commit

# Assume bundler is being used, therefore
# the `install` step will run `bundle install` by default.
script: ./script/cibuild

# branch whitelist, only for GitHub Pages
branches:
  only:
  - site

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of
# using test creds for now. Update with secure creds.
deploy:
  provider: gcs
  access_key_id: "GOOGEMQKVIBFZ3HAPXWF"
  secret_access_key: "xEnnGXm3NsnphO9EOoqRqf0rweQ3FOizkyA8fGx/"
  bucket: "tests.sonnetmedia.org"
  acl: public-read
  skip_cleanup: true
  local_dir: _site
  on:
    branch: site